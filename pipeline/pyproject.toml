[project]
name = "holo-pipeline"
version = "0.1.0"
description = "The hololive-ocg-wiki data pipeline — scrape, translate, build card data."
requires-python = ">=3.12"
dependencies = [
    "holo-schema",
    "beautifulsoup4>=4.13",
    "requests>=2.32",
    "openai>=1.107",
    "pillow>=11.0",
    "python-dotenv>=1.1",
    "tqdm>=4.67",
    "typer>=0.15",
]

[project.optional-dependencies]
# Only `holo-data publish` needs an S3 client, and only the maintainer can publish —
# it requires Cloudflare credentials no contributor has (D14). boto3 is 27 MB against a
# ~53 MB environment, so it is not everyone's tax to pay: `uv sync --extra publish`.
publish = ["boto3>=1.35"]

[project.scripts]
holo-data = "holo_data.cli:app"

[tool.uv.sources]
holo-schema = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/holo_data"]

[tool.hatch.build.targets.wheel.force-include]
"src/holo_data/translate/prompts.json" = "holo_data/translate/prompts.json"
